/**
 * Vitest global setup.
 * Imports @testing-library/jest-dom matchers (toBeInTheDocument, etc.)
 * so they're available in every test file without explicit imports.
 */
import '@testing-library/jest-dom';
